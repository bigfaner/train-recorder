---
id: "1.2"
title: "Database Schema & Domain Models"
priority: "P0"
estimated_time: "3h"
dependencies: ["1.1"]
status: pending
breaking: true
noTest: false
mainSession: false
scope: backend
---

# Database Schema & Domain Models

## Description

Create SQLDelight `.sq` file with all 19 tables from `design/schema.sql`. Create domain model data classes for all entities. Create all enums. Create DB-to-Domain mapper functions.

### Tables (19)

Exercise, TrainingPlan, TrainingDay, TrainingDayExercise, TrainingDaySetConfig, WorkoutSession, WorkoutExercise, ExerciseSet, WorkoutFeeling, ExerciseFeeling, PersonalRecord, WeightSuggestion, BodyMeasurement, OtherSportType, OtherSportMetric, OtherSportRecord, OtherSportMetricValue, TimerState, UserSettings.

### Domain Model Data Classes

Exercise, TrainingPlan, TrainingDay, TrainingDayExercise, TrainingDaySetConfig, WorkoutSession, WorkoutExercise, ExerciseSet, WorkoutFeeling, ExerciseFeeling, PersonalRecord, WeightSuggestion, BodyMeasurement, OtherSportType, OtherSportMetric, OtherSportRecord, OtherSportMetricValue, TimerState, UserSettings.

### Enums

WorkoutStatus, ExerciseStatus, TrainingType, WeightUnit, ExerciseCategory, ScheduleDayType, SuggestionHint, ExportFormat.

### Mappers

DB-to-Domain and Domain-to-DB mapper functions for each entity.

## Reference Files

- `design/schema.sql` — Full SQL schema with all CREATE TABLE statements
- `design/er-diagram.md` — Entity relationship diagram

## Acceptance Criteria

- [ ] All 19 CREATE TABLE statements execute successfully via SQLDelight
- [ ] All domain model data classes compile
- [ ] All DB-to-Domain mapper functions convert correctly
- [ ] All Domain-to-DB mapper functions convert correctly
- [ ] All enums have complete value sets matching schema constraints
- [ ] SQLDelight generates query accessor classes without errors

## User Stories

No direct user story mapping (data layer).

## Notes

Follow column names from schema.sql exactly (`display_name`, `record_date`, `workout_status`, etc. — no SQL reserved keywords). Use `kotlinx-datetime` types (`Instant`, `LocalDate`) for date/time fields in domain models.
