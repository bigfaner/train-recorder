---
id: "T-test-3"
title: "Run e2e Tests"
priority: "P1"
estimated_time: "30min-1h"
dependencies: ["T-test-2"]
status: pending
---

# Run e2e Tests

## Description

Call `/run-e2e-tests` skill to execute the generated test scripts and produce a results report.

## Reference Files

- `tests/e2e/features/train-recorder/` — Test scripts generated by T-test-2
- `tests/e2e/features/train-recorder/results/latest.md` — Output: test results report

## Acceptance Criteria

- [ ] `tests/e2e/features/train-recorder/results/latest.md` exists
- [ ] All tests pass (status = PASS in latest.md)

## User Stories

No direct user story mapping. This is a standard test execution task.

## Implementation Notes

1. Run `/run-e2e-tests` skill
2. Read `tests/e2e/features/train-recorder/results/latest.md` to determine outcome
3. Run: `just test-e2e --feature train-recorder`

**If tests fail**:
- Mark this task `blocked`: `task status T-test-3 blocked`
- Create fix tasks per root cause
- After fix tasks complete, T-test-3 re-runs

**Do NOT** attempt to fix failures inline — create fix tasks.
