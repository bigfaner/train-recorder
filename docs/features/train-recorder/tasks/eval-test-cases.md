---
id: "T-test-1b"
title: "Evaluate e2e Test Cases"
priority: "P1"
estimated_time: "30min"
dependencies: ["T-test-1"]
status: pending
mainSession: true
---

# Evaluate e2e Test Cases

## Description

Call `/eval-test-cases` skill to evaluate test-cases.md for downstream executability. This quality gate ensures test cases can directly drive `/gen-test-scripts` to produce runnable scripts.

This task runs in the **main session** (`mainSession: true`) because it needs to spawn doc-scorer and doc-reviser subagents.

## Reference Files

- `testing/test-cases.md` — Test case document (generated by T-test-1)
- `prd/prd-spec.md` — PRD spec
- `prd/prd-user-stories.md` — User stories

## Acceptance Criteria

- [ ] `testing/eval/report.md` exists with final score
- [ ] Final score >= 90 (target)
- [ ] No Step Actionability dimension score < 20

## User Stories

No direct user story mapping. This is a standard test evaluation task.

## Implementation Notes

1. Run `/eval-test-cases` skill with `--target 90 --iterations 6`
2. Review the final report
3. If target not reached after 6 iterations, mark this task as blocked

## Main Session Instructions

This section is read by `run-tasks` dispatcher when `mainSession: true`. Execute exactly these steps:

1. **Invoke skill**: `Skill(skill="forge:eval-test-cases", args="train-recorder")`
2. **Check outcome**: Read `docs/features/train-recorder/testing/eval/report.md`, extract target status
3. **Record result**:
   - Target reached → `task record T-test-1b` with status `completed`
   - Target NOT reached → `task status T-test-1b blocked`
