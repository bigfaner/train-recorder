---
id: "1.1"
title: "KMP Project Scaffold"
priority: "P0"
estimated_time: "3h"
dependencies: []
status: pending
breaking: true
noTest: false
mainSession: false
scope: all
---

# KMP Project Scaffold

## Description

Initialize KMP project with Compose Multiplatform, SQLDelight, Koin, kotlinx-datetime, kotlinx-serialization. Create project structure per tech-design: `shared/` (commonMain, androidMain, iosMain), `androidApp/`, `iosApp/`. Configure all `build.gradle.kts` files, `settings.gradle.kts`. Set up Koin DI module definitions. Create Android Activity with Compose `setContent`, iOS entry point.

## Reference Files

- `design/tech-design.md` — Architecture, Project Structure, Dependencies sections

## Acceptance Criteria

- [ ] Project compiles on Android
- [ ] All dependencies resolve (Compose Multiplatform, SQLDelight, Koin, kotlinx-datetime, kotlinx-serialization)
- [ ] Koin modules load without error
- [ ] Empty Compose content renders on both Android and iOS
- [ ] Project structure matches tech-design layout (shared/commonMain, shared/androidMain, shared/iosMain, androidApp/, iosApp/)
- [ ] Root `settings.gradle.kts` includes all subprojects
- [ ] Android Activity launches with Compose `setContent`
- [ ] iOS entry point configured for Kotlin/Native

## User Stories

No direct user story mapping (infrastructure).

## Notes

This is a greenfield project — create everything from scratch. Ensure Gradle wrapper is included and `./gradlew build` succeeds.
