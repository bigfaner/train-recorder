---
status: "completed"
started: "2026-05-09 00:39"
completed: "2026-05-09 00:48"
time_spent: "~9m"
---

# Task Record: 2.2 CalendarComputer Service

## Summary
Implemented CalendarComputer service with computeMonth, computeDay, getTodayPlan, skipTrainingDay, unskipTrainingDay, getSkippedDates, and getConsecutiveSkips methods. Supports weekly_fixed (weekday-to-training-day mapping) and fixed_interval (cycling through training days with configurable rest gaps) scheduling modes. Skipped dates persisted as JSON in user_settings. Day type resolution prioritizes: workout session > skipped > other sport > training > rest.

## Changes

### Files Created
- src/services/calendar-computer.ts
- __tests__/services/calendar-computer.test.ts

### Files Modified
- src/types/index.ts
- src/db/repositories/other-sport.repo.ts

### Key Decisions
- 2.2: CalendarComputer interface updated to include planBizKey parameter on skipTrainingDay/unskipTrainingDay (skipped dates are per-plan, stored as skipped_dates_{planBizKey} in user_settings)
- 2.2: fixed_interval mode uses deterministic day-1-based cycle computation (no dependency on plan start date)
- 2.2: CalendarComputerServiceImpl typed as alias of CalendarComputer (no empty interface)
- 2.2: Day type priority: completed session > skipped > other_sport (on rest day) > training > other_sport (on training day) > rest
- 2.2: Completed workout session overrides skip status (user can unskip by doing the workout)
- 2.2: Added findByDateRange to OtherSportRepo for batch-loading sport records in computeMonth

## Test Results
- **Passed**: 283
- **Failed**: 0
- **Coverage**: 93.5%

## Acceptance Criteria
- [x] computeMonth(year, month, plan, days) returns CalendarDay[] for all days in the month
- [x] weekly_fixed mode: maps weekday -> training_day_biz_key from plan.weekly_config JSON
- [x] fixed_interval mode: cycles through training days with rest_days gaps between
- [x] Past dates with WorkoutSession show actual status (completed/completed_partial)
- [x] Past dates without session but plan has training -> dayType='training' (user may have skipped)
- [x] skipTrainingDay(date) stores skipped date in user_settings JSON array
- [x] unskipTrainingDay(date) removes from skipped list and restores assignment
- [x] getConsecutiveSkips(planBizKey, beforeDate) returns consecutive skip count
- [x] Other sport records displayed on correct dates
- [x] Unit tests: weekly_fixed and fixed_interval scheduling, skip/unskip, consecutive skips

## Notes
无
